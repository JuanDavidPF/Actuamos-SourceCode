import { StyleSheet } from "react-native";
import { AppColors } from "../../config/AppColors";

export const BottomNavbarIconStyles = StyleSheet.create({
  tabIcon: {
    width: 44,
    height: 44,
  },
});
